//Objet case
function Case(id, etat, arme, joueur, clic) {
    this.id = id;
    this.etat = etat;
    this.arme = arme;
    this.joueur = joueur;
    this.clic = clic;
}

//Générer no access cases
function noAccessCases() {
    var nb_cases = 0
    for (nb_cases; nb_cases < 15; nb_cases++) {
    return Math.floor(Math.random() * 99);}
}
}

